export const TimerEvents = {
    tick: "tick",
    currentTimer :'currentTimer',
    timerStart: "timerStart",
    timerStop: "timerStop",
    timerReset: "timerReset",
};

export const randomUser = {
    deviceId: 1,
    userId: 2,
    // deviceId: "device" + Math.floor(Math.random() * 100 + 1),
    // userId: Math.floor(Math.random() * 100 + 1),
};
